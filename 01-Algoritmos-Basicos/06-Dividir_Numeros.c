// Dividir dois números
#include <stdio.h>

int main() {
    float a = 15.0;
    float b = 3.0;
    float resultado = a / b;

    printf("Divisão: %.2f / %.2f = %.2f\n", a, b, resultado);
    return 0;
}