#include <stdio.h>  // Biblioteca padrão de entrada/saída

// Função principal - ponto de entrada do programa
int main() {
    // Instruções do programa
    printf("Hello, World!\\n");  // Função de saída

    return 0;  // Indica sucesso (0 = OK)
}